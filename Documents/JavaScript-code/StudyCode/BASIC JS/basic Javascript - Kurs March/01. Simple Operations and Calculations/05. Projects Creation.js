function solve(architektName, numProjets){
let hoursNeeded = numProjets * 3
    console.log(`The architect ${architektName} will need ${hoursNeeded} hours to complete ${numProjets} project/s.`)
}
solve("George", 4);