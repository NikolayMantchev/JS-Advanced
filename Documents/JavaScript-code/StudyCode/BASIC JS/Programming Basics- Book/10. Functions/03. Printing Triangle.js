function printLine(start = 1, end) {
    let line = "";
    for (let i = start; i <= end; i++) {
        line += i + "";
    }
    console.log(line);


    for (let i = 1; i < n; i++) {   // n ?
        printline(1, i);
        printline(1, num);    // num ?
    }
    for (let i = n - 1; i > 0; i--) {    // n ?
        printLine(1, i)
    }
}
printLine([ '3' ])

//0/100